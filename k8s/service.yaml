apiVersion: v1
kind: Service
metadata:
  name: movie-recommender-service
spec:
  type: NodePort
  selector:
    app: movie-recommender
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007